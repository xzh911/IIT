name: Test macOS Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: macos-latest

    steps:
      - name: Check macOS
        run: sw_vers

      - name: Check Xcode
        run: xcodebuild -version
